<h1>Tasks#index</h1>
<% @tasks.each do |t| %>
  <h2><%= t.name %></h2>
  <p><%= t.description %></p>
  <%= link_to "Delete", task_path(t),
      method: :delete,
      data: { confirm: "Are you sure?" } %>
<% end %>
